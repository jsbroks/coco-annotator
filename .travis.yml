matrix:
  include:
#    - language: python
#      python:
#        - 3.6
#      before_script:
#        - pip install -r requirements.txt
#        - pip install pycocotools
#      script: pytest

    - language: node_js
      node_js:
        - 8
        - node
      cache: npm
      before_install:
        - npm install -g npm@latest
        - cd client
      install:
        - npm i
      script: npm test


